[project]
name = "go"
dynamic = ["version"]
dependencies = [
    "matplotlib",
    "numpy",
    "psutil",
    "torch==2.7.0",
    "whr",
]

[project.optional-dependencies]
dev = [
    "pre-commit",
    "ruff",
]
